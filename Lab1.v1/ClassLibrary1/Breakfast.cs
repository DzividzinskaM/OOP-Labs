using System;
using System.Collections.Generic;
using System.Text;

namespace ClassLibrary1
{
   internal class Breakfast : Meal
   {
        internal override void GetRandomMenu()
        {
            Menu menu = new Menu();
            foreach (var value in menu.getMenu())
            {
                if (value.TypeMeal = )
            }
        }
    }
}
